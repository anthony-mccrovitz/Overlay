# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.eggs/
venv/
.venv/

# Environment
.env

# Data (large datasets — download separately)
data/kaggle/
data/kenpom/
data/cache/

# Generated output — exclude large files, keep JSON picks for website
output/**/*.png
output/**/*.html
output/**/*.md
output/**/caption_*.txt
# Keep: picks.json, props.json, nrfi.json, picks_card.json

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Model artifacts
# NOTE: trained model .pkl/.joblib/.cbm are now COMMITTED so GitHub Actions can
# run picks generation in the cloud (no laptop needed). They're small (~5M total).
# Re-add the ignores below only if models grow large enough to warrant git-lfs.
catboost_info/

# Node / Next.js
web/node_modules/
web/.next/
web/.env.local

# Jupyter
.ipynb_checkpoints/

# gstack / claude local settings
.claude/
.gstack/
dashboard/node_modules/
dashboard/.next/
.vercel

# archived frontends — never track their deps/build output
legacy/**/node_modules/
legacy/**/.next/

# Sherdog fighter cache — regenerable, thousands of files
data/ufc/sherdog/
